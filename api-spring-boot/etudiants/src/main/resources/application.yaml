spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/etudiants
    username: postgres
    password: amal
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

server:
  port: 8080