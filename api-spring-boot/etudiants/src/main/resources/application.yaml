spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/etudiants
    username: postgres
    password: amal
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080